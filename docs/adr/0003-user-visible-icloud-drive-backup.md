# User-Visible iCloud Drive Backup

Correction Notebook must provide a backup file in a user-visible iCloud Drive folder rather than relying only on app-private local persistence or opaque device backup. The product is local-first and does not include account sync in the MVP, so a visible iCloud Drive backup is the minimum recoverability promise for students who accumulate long-lived mistake history.
